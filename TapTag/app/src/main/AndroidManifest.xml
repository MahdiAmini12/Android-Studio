<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TapTag">

        <!-- صفحه لاگین -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- پنل دانشجو -->
        <activity
            android:name=".StudentPanelActivity"
            android:label="Student Panel" />

        <!-- پنل استاد -->
        <activity
            android:name=".ProfessorPanelActivity"
            android:label="Professor Panel" />

        <!-- پنل کارمند -->
        <activity
            android:name=".StaffPanelActivity"
            android:label="Staff Panel" />

        <!-- صفحه SetupDatabase (اختیاری) -->
        <activity android:name=".SetupDatabaseActivity" />

    </application>

</manifest>
